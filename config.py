import os

# Configuration settings
CONFIG = {
    "app_name": "MyPythonApp",
    "version": "1.0.0",
    "log_level": "INFO",
    "log_file": "app.log"
}

# You can load from environment variables or files here
# For example:
# CONFIG["database_url"] = os.getenv("DATABASE_URL", "sqlite:///default.db")